{
  "name": "blockchain",
  "version": "4.0.0",
  "description": "Field Commander Smart Contracts — CitizenLedgerDTA + EvidenceLedger",
  "scripts": {
    "compile":         "hardhat compile",
    "test":            "hardhat test",
    "node":            "hardhat node",
    "deploy:local":    "hardhat run scripts/deploy.ts --network localhost",
    "deploy:amoy":     "hardhat run scripts/deploy.ts --network amoy",
    "deploy:polygon":  "hardhat run scripts/deploy.ts --network polygon",
    "e2e:local":       "hardhat run scripts/e2e-happy-path.ts --network localhost",
    "e2e:amoy":        "hardhat run scripts/e2e-happy-path.ts --network amoy",
    "typechain":       "hardhat typechain"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.5"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
